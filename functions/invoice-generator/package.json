{
  "name": "invoice-generator",
  "version": "1.0.0",
  "description": "Cloud Function to generate invoices for orders",
  "main": "index.js",
  "scripts": {
    "start": "npx @google-cloud/functions-framework --target=generateInvoice --port=8080",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gcloud functions deploy generateInvoice"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.0"
  }
}
